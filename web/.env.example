# Copy to .env.local and fill in values

# Single admin password for accessing edit/sync features
ADMIN_PASSWORD_HASH=

# Secret for session cookie encryption (generate with: openssl rand -base64 32)
SESSION_SECRET=

# Path to the SQLite database file (inside the container, map a volume here)
DATABASE_PATH=./data/babydatalog.db
