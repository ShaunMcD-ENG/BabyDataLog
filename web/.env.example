# Copy to .env.local and fill in values

# Secret for session cookie encryption (generate with: openssl rand -base64 32)
SESSION_SECRET=

# Path to the SQLite database file (inside the container — map a volume to /data)
DATABASE_PATH=/data/babydatalog.db
